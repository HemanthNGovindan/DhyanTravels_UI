import { PostdateformatPipe } from './pipe';

describe('PostdateformatPipe', () => {
  it('create an instance', () => {
    const pipe = new PostdateformatPipe();
    expect(pipe).toBeTruthy();
  });
});
