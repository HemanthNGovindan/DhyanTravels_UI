import { ViewmoreDirective } from './viewmore.directive';

describe('ViewmoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewmoreDirective();
    expect(directive).toBeTruthy();
  });
});
