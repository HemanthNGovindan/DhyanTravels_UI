import { ViewNotifyDirective } from './view-notify.directive';

describe('ViewNotifyDirective', () => {
  it('should create an instance', () => {
    const directive = new ViewNotifyDirective();
    expect(directive).toBeTruthy();
  });
});
